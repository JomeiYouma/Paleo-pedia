# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Images uploadées : vivent dans paleo-uploads en prod (hors du code).
# Ne pas committer ni builder les images locales.
public/images/

# Guides PDF (lourds, ~8-9 Mo) : générés localement, déployés à la main.
# Ils restent sur le disque et sont embarqués dans dist au build local.
public/guide-site-dedie-*.pdf

# Artefacts de déploiement (ZIPs SFTP)
*.zip

# Outils de dev locaux jetables (probes BDD, générateurs de migration ponctuels).
# Ne pas committer ni déployer — probe_press.mjs lit DATABASE_URL dans .env.
probe_*.mjs
gen_*.mjs

# Secrets — ne JAMAIS committer le .env réel
.env
.env.local
.env.*.local
# .env.production.example est volontairement laissé (pas de secrets)

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
